
<% connections.each_with_index do |connection, index| %>
<%=render partial: '/categories/category',
          locals: { category: connection } %>  <% if (connections.length - 1 > index) %>,<%end%>
<% end %>
