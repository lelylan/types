{
  "name": "<%=type.name%>",
  "uri": "<%=type.uri%>",
  "id": "<%=type.id.as_json%>",
  "public": <%=raw type.public.to_json%>,
  "created_from": { "uri": "<%=type.created_from%>" },
  "categories": [ <%=render partial: "/types/type_categories",
                            locals: { connections: type.connected_categories } %>  ],
  "properties": [ <%=render partial: "/types/type_properties",
                            locals: { connections: type.connected_properties } %>  ],
  "functions": [ <%=render partial: "/types/type_functions",
                           locals: { connections: type.connected_functions } %>  ],
  "statuses": [ <%=render partial: "/types/type_statuses",
                           locals: { connections: type.connected_statuses } %>  ]
}
